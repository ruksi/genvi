version: 2
updates:

  - package-ecosystem: github-actions
    directory: /
    schedule: {interval: daily}
    labels:
      - 🤖 Robot
      - 🧹 Upkeep

  # NB: cannot rely on Dependabot Python updates as using multiple manifests (e.g. requirements-dev.in)
