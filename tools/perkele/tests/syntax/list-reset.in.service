# https://www.freedesktop.org/software/systemd/man/systemd.syntax.html
# > Often, multiple settings form a list, and setting to an empty value "resets" [the list]...
#
# Thus, maintain empty values as they are in the config.
[Service]
Type=oneshot
ExecStart=
ExecStart=one
# comments should not introduce empty values, or neither should empty lines
ExecStart=two
ExecStart=
ExecStart=three

ExecStart=four
